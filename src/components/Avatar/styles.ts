import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	oval: {
		width: 100,
		height: 90,
		borderRadius: 70,
		borderWidth: 5,
		borderColor: "#dc9",
		transform: [{ scaleY: 2 }],
	},
});
