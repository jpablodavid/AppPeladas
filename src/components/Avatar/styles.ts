import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: {
		flex: 1,
		alignItems: 'center',
	},
	foto: {
		width: 100,
		height: 100,
		backgroundColor: 'red',
	},
});
